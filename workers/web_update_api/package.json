{
  "name": "momoweb-update-api",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply momoweb-update-db --local",
    "db:migrate": "wrangler d1 migrations apply momoweb-update-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
