

cd web_support
python3 -m http.server 8000


http://localhost:8000


http://localhost:8000/apps/todo-calendar/



git add .
git commit -m "web"
git push

Cmd + Option + I


git name/emailが未設定で自動生成(momo dai <momose@momonoMacBook-Air.local>)になってる。
GitHubアカウントと紐付けたい場合は設定する:

git config --global user.name "あなたの名前"
git config --global user.email "あなたのメールアドレス"

設定済みで過去コミットも直したい場合: git commit --amend --reset-author